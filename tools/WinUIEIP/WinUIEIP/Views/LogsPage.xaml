<Page x:Class="EulerLink.Views.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ScrollViewer>
        <StackPanel Margin="20,16">
            <TextBlock Text="Device Logs" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,16"/>

            <Border x:Name="NotConnectedWarningBorder"
                    BorderBrush="#0066CC"
                    BorderThickness="2"
                    CornerRadius="4"
                    Background="#E6F2FF"
                    Padding="12"
                    Margin="0,0,0,16"
                    Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ℹ️" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Not connected to device. Please connect to a device in the sidebar to view logs."
                               FontWeight="SemiBold"
                               Foreground="#003366"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <StackPanel x:Name="LogsContentPanel">
                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="4" 
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        Padding="0"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="12,12,12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Device Log Buffer" 
                                       FontWeight="SemiBold" 
                                       FontSize="16"
                                       VerticalAlignment="Center"/>
                            <Button x:Name="RefreshButton"
                                    Content="Refresh"
                                    Click="RefreshButton_Click"
                                    Style="{StaticResource TealButtonStyle}"
                                    Grid.Column="1"
                                    Padding="12,8"
                                    Margin="8,0,0,0"/>
                        </Grid>
                        <StackPanel Margin="12,0,12,12">
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Log Size:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <TextBlock x:Name="LogSizeTextBlock" Text="0 bytes" Grid.Column="1"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Total Size:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <TextBlock x:Name="TotalSizeTextBlock" Text="0 bytes" Grid.Column="1"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,0,8,0"/>
                                <TextBlock x:Name="TruncatedTextBlock" Text="Not truncated" Grid.Column="1"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" 
                        CornerRadius="4" 
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        Padding="0"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Log Content" 
                                   FontWeight="SemiBold" 
                                   FontSize="16" 
                                   Margin="12,12,12,8"/>
                        <ScrollViewer Height="500" 
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      Margin="12,0,12,12">
                            <TextBlock x:Name="LogsTextBlock"
                                       FontFamily="Consolas"
                                       FontSize="11"
                                       TextWrapping="NoWrap"
                                       IsTextSelectionEnabled="True"
                                       Text="Loading logs..."/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>

